// Напишите функцию для сравнения двух
// объектов, чтобы определить, содержит ли
// первый из них эквивалентные значения
// свойств по сравнению со вторым

const equal = (par1, par2) => {
	return par1 == par2
}

console.log(equal(2, "2"))